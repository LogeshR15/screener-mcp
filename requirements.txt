# Full install: core + document analysis. Installing this file pulls in
# torch (via sentence-transformers), roughly 1-2GB.
# For a lightweight install, use requirements-core.txt instead.
#
# Core
mcp>=1.0.0,<2.0.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Document analysis (optional — required for analyze_annual_report, analyze_earnings_call)
# Install with: pip install screener-mcp[ai]   or   pip install pdfplumber sentence-transformers chromadb
pdfplumber>=0.11.0
chromadb>=0.5.0
sentence-transformers>=3.0.0
